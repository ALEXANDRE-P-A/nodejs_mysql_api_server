CREATE TABLE IF NOT EXISTS todos (
  id INT(11) NOT NULL AUTO_INCREMENT PRIMARY KEY,
  content VARCHAR(256) NOT NULL,
  editing BOOLEAN NOT NULL
);

ALTER TABLE todos CHANGE editing editing BOOLEAN NOT NULL;